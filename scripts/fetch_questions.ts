import { createClient } from '@supabase/supabase-js'; import * as dotenv from 'dotenv'; import * as fs from 'fs'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL!, process.env.VITE_SUPABASE_ANON_KEY!); async function main() { const { data, error } = await supabase.from('questions').select('*').order('type').order('order_index'); if (error) console.error(error); else fs.writeFileSync('questions_dump.json', JSON.stringify(data, null, 2)); } main();